workflows:
  android-workflow:
    name: Build Android APK
    max_build_duration: 120
    environment:
      java: 11
      node: 18
    scripts:
      - name: Install Bun
        script: npm install -g bun
      - name: Install dependencies
        script: bun install
      - name: Build web app
        script: bun run build
      - name: Sync Capacitor
        script: bunx cap sync android
      - name: Build APK
        script: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease
    artifacts:
      - android/app/build/outputs/apk/release/app-release-unsigned.apk
    publishing:
      email:
        recipients:
          - user@example.com
        notify:
          success: true
          failure: true
